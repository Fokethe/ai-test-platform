# encoding: utf-8
# -*- coding: utf-8 -*-

# Cline 智能助手规则配置（精简版）
# 版本: 2.0 | 适用: ai-test-platform 项目
# 核心 Skill: 10个 | 完整 Skill 库: .clinerules.skill-library.md

================================================================================
## Skill 0: Skill 编排器 (Skill Orchestrator) - 意图识别
================================================================================

【触发方式】: 每轮对话优先执行，自动识别用户意图并触发 Skill 序列

【意图识别规则】:
当用户输入以下关键词时，自动执行对应的 Skill 序列：

| 用户意图 | 触发关键词 | 自动执行的 Skill 序列 |
|---------|-----------|---------------------|
| 新项目启动 | "开始新项目" "新项目" "从零开始" | cost-control → socratic-inquiry → task-planner → doc-system |
| 功能开发 | "开发新功能" "添加功能" "实现功能" | cost-control → task-planner → workflow → code-review → doc-system |
| TDD 循环 | "TDD 模式" "循环开发" "先写测试" | cost-control → tdd-loop (红→绿→重构) |
| 代码重构 | "重构" "优化代码" "重构 {文件名}" | cost-control → code-review → task-planner → code-refactor |
| Bug 修复 | "修复bug" "debug" "报错了" | cost-control → debug-diagnosis → danger-signals → code-review |
| 设计还原 | "还原设计" "设计稿" "截图转代码" | cost-control → visual-coding → task-planner → code-review |
| 文档整理 | "整理文档" "处理文档" "文档太大" | cost-control → doc-processor → 整合 |
| 代码提交 | "提交代码" "commit" "写commit" | cost-control → code-review → doc-system → git-commit |
| 健康检查 | "检查健康" "检查健康度" "状态检查" | cost-control → context-management → danger-signals → code-review → cost-control |
| 深度修复 | "BugHunter" "深度修复" "自动修复" | cost-control → bughunter-loop |
| 多任务调度 | "使用 subagent" "并行处理" | cost-control → multi-task-scheduler |
| 自动清理 | "清理项目" | cost-control → auto-cleanup |

【特殊指令】:
- "加载 Skill XX" → 从 .clinerules.skill-library.md 加载指定 Skill
- "显示所有 Skill" → 列出核心 Skill 和备用 Skill 清单

================================================================================
## Skill 1: 成本控制协议 (Cost Control)
================================================================================

【触发方式】: 每轮对话自动执行；用户说 "/cost" 立即触发

【规则】:
- 读取文件 > 5,000 字符时: "📊 读取此文件预估消耗: ${cost} (约 {n} tokens)"
- 批量修改 > 3 个文件时: "⚠️ 批量修改预估消耗: ${cost}，建议分批处理"
- 单轮累计 > $0.05 时: 必须询问 "预估消耗 ${cost}，是否继续? [Y/N]"
- 单轮累计 > $0.10 时: 提示 "💡 建议开启新对话或执行 /compact 压缩上下文"
- 上下文占用 > 60% 时: 提示 "💡 上下文即将满载，建议开新对话"

【快捷指令】: `/cost` - 显示当前累计消耗

================================================================================
## Skill 2: 大文档分段处理协议 (Doc Processor)
================================================================================

【触发方式】: 文件 > 30,000 字符自动激活；或说"整理文档"、"处理大文件"

【执行流程】:
1. **立即停止**: "检测到大型文档 (约 {n} 字符)，触发分段协议"
2. **结构分析**: 输出文档地图，建议分为 {x} 段 (每段 5,000-8,000 字符)
3. **制定计划**: 分块方案（块1、块2...每块主题）
4. **逐块处理**: 等待用户发送第 1 块，处理完再要第 2 块
5. **最终整合**: 所有块完成后输出合并方案和去重结果

【禁止事项】:
- 禁止一次性读取 > 5 万字
- 禁止未制定分块计划前直接回答内容摘要

【快捷指令】: `/plan` - 重新生成分段/任务计划

================================================================================
## Skill 3: 任务拆解与规划协议 (Task Planner)
================================================================================

【触发方式】: 复杂需求含"实现"、"开发"、"添加功能"等多步骤关键词时

【强制步骤】:
1. **暂停编码**: 不要立即写代码
2. **输出计划**:
```
📋 执行计划: {任务名}
预估总消耗: ${cost} | 预计 {n} 轮对话

阶段 1: 准备与调研
- 分析现有代码结构
- 识别依赖关系

阶段 2: 实施
- {步骤1} → 产出: {文件}
- {步骤2} → 产出: {文件}

阶段 3: 验证
- 运行测试/检查错误
```
3. **等待确认**: "是否按此计划执行? 或有调整?"
4. **分阶段执行**: 每完成一个阶段输出进度，询问 "继续下一阶段?"

【快捷指令】: `/next` - 继续下一阶段/下一块内容

================================================================================
## Skill 4: 代码审查协议 (Code Review)
================================================================================

【触发方式】: "审查代码"、"review"、"检查这段代码"、"看看有什么问题"

【检查清单】:
- [ ] **可读性**: 变量名语义化? 函数长度 < 50 行?
- [ ] **重复代码**: 是否有可提取的公共逻辑?
- [ ] **错误处理**: 异步错误、空值、边界情况是否处理?
- [ ] **性能**: 是否有循环嵌套、重复计算?
- [ ] **安全**: SQL 注入、XSS、敏感信息硬编码?
- [ ] **类型安全**: TypeScript 是否有 any 类型滥用?

【输出格式】:
```
🔍 审查结果: {文件名} 风险等级: 🔴 高 / 🟡 中 / 🟢 低

主要问题:
1. [问题] - 建议: [具体修改]
2. ...

优化建议:
- 建议提取函数: {函数名} (重复出现在第 X,Y 行)
- 建议重命名: {旧} → {新}

✅ 可保留部分: {写得好的地方}
```

================================================================================
## Skill 5: 需求审问与项目启动 (Socratic Inquiry & Project Launch)
================================================================================

【触发方式】: "新项目启动"、"开始新项目"、"我要做..."、需求模糊时

【执行流程】:
1. **暂停编码**: 不要立即写代码
2. **苏格拉底式提问**:
   - 每次只问 1 个最关键的问题
   - 从"用户打开应用第一眼看到什么"开始
   - 根据回答深入追问细节（5-8轮）
3. **文档初始化**: 自动创建 KIMI.md 和 progress.md
4. **四步工作流**:
   - Step 1: 审问 (5-10 分钟)
   - Step 2: 拆解 (生成 PRD、技术方案、实施计划)
   - Step 3: 实现 (迭代循环，一次一个功能)
   - Step 4: 验收 (Checklist: 代码审查/规范检查/功能验证/更新进度)

【审问问题库】:
- 用户第一眼看到什么？第一步操作什么？
- 核心功能有哪些？优先级排序？
- 数据存储在哪里？本地还是云端？
- 目标用户是谁？使用场景是什么？

================================================================================
## Skill 6: 文档双轨与提交系统 (Doc System & Git Commit)
================================================================================

【触发方式】: "创建项目文档"、"初始化文档"、"写commit"、"提交代码"

【双文档结构】:
1. **KIMI.md** (AI 操作手册)
   - 位置: 项目根目录
   - 内容: 技术栈、文件路径约定、技术铁律、当前目标
   - 更新: 每次会话前读取，目标变更时更新

2. **progress.md** (进度追踪)
   - 位置: 项目根目录
   - 内容: 已完成、当前进行、下一步、技术债务
   - 更新: 每完成一个功能后更新

【Git 提交规范】:
```
<type>(<scope>): <subject>

<body>

<footer>
```

【Type 定义】: feat(新功能) | fix(Bug修复) | docs(文档) | style(格式) | refactor(重构) | test(测试) | chore(构建)

【规则】:
1. Subject ≤ 50 字符，使用祈使句 ("添加" 而非 "添加了")
2. Body 说明改动原因 (why)，每行 ≤ 72 字符
3. Footer: 破坏性变更标注 BREAKING CHANGE: {描述}

================================================================================
## Skill 7: 调试诊断协议 (Debug Diagnosis)
================================================================================

【触发方式】: "修复这个错误"、"分析原因"、"运行报错"、"报错了"

【万能公式模板】:
用户需要提供:
1. 问题描述: [一句话描述现象]
2. 错误信息: ```[完整报错，包括堆栈]```
3. 相关代码: ```[相关代码片段]```
4. 预期行为: [应该发生什么]
5. 实际行为: [实际发生什么]

【分析步骤】:
1. **根本原因分析**: 错误产生的根本原因是什么？
2. **修复方案**: 有哪些修复方案？推荐哪个？
3. **修复代码**: 给出修复后的完整代码 (diff 格式)
4. **预防措施**: 如何预防类似问题？

【输出格式】:
```
🔍 错误分析:
- 错误类型: [如 TypeError]
- 根本原因: [解释]
- 发生位置: [文件:行号]

💡 修复方案:
推荐: [方案描述]
替代: [其他方案]

📝 修复代码:
[diff 对比]

🛡️ 预防建议:
- [建议1]
- [建议2]
```

================================================================================
## Skill 8: 危险信号检测 (Danger Signals)
================================================================================

【触发方式】: 开发过程中自动监控

【危险信号清单】:
- [ ] **AI 开始重复之前的内容** → 上下文满了，必须新建会话
- [ ] **生成的代码无法运行，修复 3 次还没好** → 回滚到上一个稳定版本
- [ ] **AI 说"根据常见做法..."** → 描述太模糊，必须进入审问模式
- [ ] **一次生成了 5 个以上文件** → 粒度太粗，要求"一次只生成一个组件"
- [ ] **代码中出现大量 any 或 @ts-ignore** → 要求"严格类型，禁止 any"
- [ ] **单轮累计消耗 > $0.05** → 询问是否继续（Skill 1）
- [ ] **上下文占用 > 60%** → 提示开新对话

【应对措施】:
```
🚨 危险信号检测: [信号描述]

建议行动:
1. [具体行动1]
2. [具体行动2]

是否执行? [Y/N]
```

================================================================================
## Skill 9: BugHunter - 自主代码诊断与修复系统
================================================================================

【触发方式】: "BugHunter"、"深度修复"、"自动修复"、"扫描修复"

【核心工作循环】:
```
while (true) {
  1. 【扫描】全面检测代码库
  2. 【分析】分类定级所有问题
  3. 【修复】生成并应用修复方案
  4. 【验证】测试确保修复有效
  5. 【回归】检查是否引入新问题
  6. 【报告】输出本次迭代结果
  7. 【决策】判断是否继续下一轮
}
```

【检测维度】:
- 功能缺陷: 逻辑错误、算法错误、业务规则违反
- 代码质量: 空指针、资源泄漏、异常处理、并发问题、性能瓶颈
- 安全漏洞: 注入攻击、XSS/CSRF、敏感信息硬编码、权限绕过
- 可维护性: 代码重复、过度复杂函数、命名不规范、缺少注释

【执行模式】:
- 模式A: 深度修复模式 (默认) - 单轮彻底修复所有问题
- 模式B: 渐进迭代模式 - 多轮小步快跑，每轮结束询问是否继续
- 模式C: 定向狙击模式 - 修复特定类型问题 (如"只修复 SQL 注入")

【快捷指令】:
- `/bughunter` - 启动 BugHunter 默认模式
- `/bughunter stop` - 停止循环
- `/bughunter next` - 立即进入下一轮
- `/bughunter report` - 生成当前状态报告

================================================================================
## Skill 10: 多任务调度器 (Multi-Task Scheduler)
================================================================================

【触发方式】: "使用 subagent"、"并行处理"、"多任务调度"、"/parallel"

【核心能力】:
1. **任务分解** - 将复杂任务拆分为独立的子任务（带优先级和依赖关系）
2. **并行调度** - 同时启动最多 5 个 subagent 处理
3. **批次管理** - 每批次完成后更新批次文档
4. **结果整合** - 合并所有 subagent 的输出，检测并解决冲突
5. **文档同步** - 批次级 + 总级双轨文档更新

【执行流程】:
1. **任务分析** - 分析主任务的分解可行性
2. **子任务定义** - 为每个子任务定义目标、输入、输出、优先级、依赖
3. **并行调度** - 使用 use_subagents 工具同时启动多个 subagent
4. **结果收集** - 收集各 subagent 的执行结果
5. **批次文档更新** - 更新本批次的 `kimi.batch-{n}.md` 和 `progress.batch-{n}.md`
6. **冲突检测** - 检测子任务间的冲突
7. **冲突解决** - 根据优先级和依赖关系解决冲突
8. **结果整合** - 合并所有子任务结果为统一格式
9. **总文档更新** - 更新总 `kimi.md` 和 `progress.md`
10. **输出交付** - 输出整合后的最终结果

【快捷指令】:
- `/parallel` - 启动多任务调度器
- `/batch next` - 进入下一批次
- `/batch status` - 查看当前批次状态

================================================================================
## 系统指令 (System Instructions)
================================================================================

【全局约束】:
1. 回复使用中文，技术术语保留英文 (如 API, Token, diff)
2. 每轮回复必须包含 Token 成本预估 (Skill 1 要求)
3. 遇到冲突规则时，优先级: Cost Control > Doc Processor > 其他
4. 上下文占用 > 60% 时主动提示: "💡 上下文即将满载，建议开新对话"
5. **所有输出文件必须包含 UTF-8 编码声明**: `# encoding: utf-8`

【快捷指令汇总】:
| 指令 | 功能 | 所属 Skill | 适用范围 |
|------|------|-----------|----------|
| `/cost` | 显示当前累计消耗 | Cost Control | 🔵 通用 |
| `/compact` | 压缩上下文 | Context Management | 🟣 Cline 专用 |
| `/next` | 继续下一阶段/下一块 | Task Planner / Doc Processor | 🔵 通用 |
| `/plan` | 重新生成计划 | Task Planner | 🔵 通用 |
| `/parallel` | 启动多任务调度 | Multi-Task Scheduler | 🟣 Cline 专用 |
| `/bughunter` | 启动深度修复 | BugHunter | 🟣 Cline 专用 |
| `/cleanup` | 自动清理项目 | AutoCleanup | 🟣 Cline 专用 |
| `/health` | 系统健康检查 | System Health Check | 🟣 Cline 专用 |
| `/inspect` | 功能深度审查 | Functionality Deep Inspector | 🟣 Cline 专用 |

> **图例说明**: 🟣 Cline 专用 | 🔵 通用（Cline + Kimi）| 🟢 Kimi 专用（见 .kimi/skills/USAGE_GUIDE.md）

【优先级规则】:
```
Cost Control (Skill 1) > Doc Processor (Skill 2) > 其他 Skill
```

【Skill 库】:
- **核心 Skill** (本文件): 10个最常用 Skill
- **备用 Skill 库**: `.clinerules.skill-library.md` (18个扩展 Skill)
- **加载方式**: 说"加载 Skill XX"或"从备用库加载 XX"

================================================================================
## 使用说明
================================================================================

**保存位置**: 项目根目录 `.clinerules`

**立即测试**:
1. 保存文件后，**重启 Cline 对话**（点 + 号新建）
2. 说"我要做一个新项目" → 应该触发 Skill 5 进入审问模式
3. 说"整理这个大文档" → 应该触发 Skill 2 进入分段处理
4. 说"启动 BugHunter" → 应该触发 Skill 9 进入深度修复
5. 说"使用 subagent 并行处理" → 应该触发 Skill 10

**加载扩展 Skill**:
- "从备用库加载 TDD 开发" → 从 .clinerules.skill-library.md 加载 Skill
- "显示所有可用 Skill" → 列出核心 Skill 和备用 Skill 清单

**建议保留的核心 Skill**: 本文件中的 10 个 Skill 已覆盖 95% 的日常开发场景

================================================================================
*配置版本: 2.0 | 最后更新: 2026-02-28 | 核心 Skill: 10个*

## Skill 11: 自动清理 (AutoCleanup) - V2.1

【触发方式】: 
- 开发完成后自动触发: "开发完成"、"功能完成"、"代码写好了"
- 测试完成后自动触发: "测试完成"、"测试结束"、"验证通过"
- 手动触发: "/cleanup"、"清理项目"、"整理文件"

【清理范围 - 文件类】:
1. **空代码文件**: 小于 50 bytes 的 .ts/.js/.tsx/.jsx 文件
2. **临时脚本**: 
   - gen*.js/ts, create*.js/ts, write*.js/ts
   - temp*.js/ts, tmp*.js/ts
3. **临时文本文件**: 
   - temp*.txt, test*.txt, output.txt, test-output.txt
   - 内容仅为简单测试字符串的文件 (如 "hello")
4. **异常文件名**: 
   - 文件名长度 = 1 的特殊字符 ({, }, [, ], $, 等)
   - 文件名包含 $null, undefined 等
   - 文件名包含括号 ( , ) , [ , ]
5. **空测试文件**: 0 bytes 或内容极少的 *.test.ts 文件
6. **重复/临时代码文件**:
   - 与主项目目录重复的部分实现 (根目录 src/, my-app/ 等)
   - JSON 格式伪装成 TS 的文件
   - 代码截断的不完整文件

【清理范围 - 目录类】:
1. **空目录**: 完全空的文件夹
2. **临时目录**: 
   - -Force/, -p/, [name]/, temp/, test/
   - import/, queue/, vector/, outdated/ (常见临时目录名)
3. **重复目录**: 
   - 根目录下与 ai-test-platform/ 内结构重复的目录

【文件分类规则】:
```
生成的代码文件 → ai-test-platform/src/lib/ai/ 对应目录
测试文件 → ai-test-platform/src/__tests__/ 对应目录
文档文件 → ai-test-platform/docs/ 对应分类目录
过程性文档 → ai-test-platform/docs/99-历史归档/
```

【执行流程】:
1. **扫描阶段**: 扫描项目根目录和关键子目录
2. **识别阶段**:
   - 识别空代码文件 (< 50 bytes)
   - 识别临时文件 (temp*, gen*, create*, write*, tmp*)
   - 识别临时文本文件 (test*.txt, *output.txt)
   - 识别异常文件名 (长度=1的特殊字符等)
   - 识别重复/部分实现的目录
   - 识别空目录
3. **确认阶段**: 生成清理确认清单，展示给用户确认
4. **执行阶段**: 
   - 删除确认的文件和目录
   - 移动可分类的生成文件到正确位置
5. **验证阶段**: 列出清理后的目录结构，确认清理成功
6. **报告阶段**: 生成详细清理报告
7. **Git 提交** (可选): 自动提交清理变更

【输出格式】:
```
🧹 自动清理报告

📊 清理统计:
├─ 删除空代码文件: {n} 个
├─ 删除临时脚本: {n} 个
├─ 删除临时文件: {n} 个
├─ 删除临时目录: {n} 个
├─ 移动分类文件: {n} 个
└─ 总计释放空间: {size}

📝 详细清单:
[文件列表...]

✅ Git 提交: {commit_hash}
```

【快捷指令】:
- `/cleanup` - 执行自动清理
- `/cleanup dry-run` - 预览清理内容（不实际删除）
- `/cleanup --no-git` - 清理但不提交 Git

【Git 提交信息模板】:
```
chore: 自动清理项目文件

- 删除空代码文件 {n} 个
- 删除临时脚本 {n} 个
- 删除临时文件 {n} 个
- 删除临时目录 {n} 个
- 移动分类文件 {n} 个

自动清理执行时间: {timestamp}
```

## Skill 12: 系统健康检查 (System Health Check)

【触发方式】: 
- 定期自动触发（每 10 轮对话）
- 手动触发: "/health"、"检查健康度"、"检查项目健康"、"项目体检"
- Intent 触发: "检查健康"、"健康检查"

【执行流程】:
```
cost-control → context-management → danger-signals → code-review → cost-control
  └── 1. 【context-management】压缩上下文，防止失忆
  └── 2. 【danger-signals】检测危险信号（上下文占用、消耗等）
  └── 3. 【code-review】检查代码健康度（测试覆盖率、类型错误、构建状态）
  └── 4. 【cost-control】统计消耗，生成健康报告
```

【检查项目】:
1. **上下文健康度**: 上下文占用率、对话轮数、历史记录长度
2. **危险信号**: 重复内容、修复次数、类型安全、消耗预警
3. **代码健康度**: 可读性、重复代码、错误处理、性能、安全、类型安全
4. **消耗统计**: 当前消耗、累计消耗、上下文占用

【输出格式】:
```
🏥 系统健康检查报告

📊 当前消耗: $0.089

[context-management] 上下文已压缩
当前进度: 测试管理功能开发完成 80%
对话轮数: 15轮

[danger-signals] 🚨 检测到信号:
- 上下文占用 > 60%
- 建议执行 /compact 或新开对话

[code-review] 健康度检查:
- 测试覆盖率: 87%
- 类型错误: 27 个（非阻塞）
- 构建状态: ✅
- 安全漏洞: 0个

💡 建议行动:
1. [建议1]
2. [建议2]
```

【快捷指令】:
- `/health` - 执行健康检查
- `/health --fix` - 自动修复可修复的问题

## Skill 13: 功能深度审查器 (Functionality Deep Inspector)

【触发方式】:
- 用户输入: "深度检查功能完整度"、"扫描空代码"、"检查功能缺失"、"/inspect"
- 自动触发: BugHunter 启动前的可选前置步骤、代码审查时检测到大量页面文件
- 健康检查时: 发现功能完整度低于阈值

【核心检查维度】:

### 1. 空代码检查
- **空文件**: 0字节、只有注释、只有导出语句
- **空函数**: 只有函数定义没有实现体
- **空页面**: 只有框架组件没有内容
- **空组件**: 只有props定义没有渲染逻辑
- **空API路由**: 只有路由定义没有处理函数

### 2. 空页面检查
- **重定向页面**: 检查是否合理，是否缺少内容
- **占位页面**: 只有"开发中"、"TODO"等占位符
- **骨架屏页面**: 只有加载状态没有实际内容
- **404/错误页面**: 是否正确配置

### 3. 缺失功能检查
- **UI占位**: 有按钮但没有onClick处理
- **菜单项**: 有菜单选项但点击无反应
- **表单提交**: 有表单但没有提交处理
- **API调用缺失**: 前端调用但后端路由不存在
- **功能入口**: 有需求文档但无对应功能入口

### 4. 功能无法实现检查
- **前后端不对齐**: API路径、方法、参数不匹配
- **模拟数据**: 使用硬编码数据代替真实API
- **TODO/FIXME**: 标记未完成的功能
- **依赖缺失**: 缺少必要的第三方库或服务
- **环境配置**: 缺少必要的环境变量或配置

【检查流程】:
```
阶段 1: 功能项识别
  └── 扫描页面/API/组件/工具函数
  └── 生成功能项清单

阶段 2: 功能点拆解
  └── 按钮: 每个按钮的点击处理
  └── 表单: 字段、验证、提交
  └── 表格: 列、排序、筛选、分页
  └── 弹窗: 打开、关闭、确认逻辑
  └── Tabs: 每个Tab的内容完整性
  └── 链接: 跳转目标有效性

阶段 3: 实现状态检查
  └── ✅ 完整实现: 有UI+有逻辑+有API
  └── ⚠️ 部分实现: 有UI但逻辑不完整
  └── ❌ 未实现: UI或功能完全缺失
  └── 🎭 模拟实现: UI存在但使用假数据

阶段 4: 前后端对齐检查
  └── 提取前端API调用
  └── 检查后端路由是否存在
  └── 检查HTTP方法是否匹配
  └── 检查请求/响应结构

阶段 5: 生成分级报告
  └── 统计概览: 功能项/功能点/实现率
  └── 高优先级: 影响核心功能
  └── 中优先级: 影响使用体验
  └── 低优先级: 锦上添花
  └── 修复建议: 具体修复方案
```

【输出格式】:
```
🔍 功能深度审查报告

📊 统计概览:
├─ 功能项总数: {n}
├─ 功能点总数: {n}
├─ 完整实现: {n} ({rate}%)
├─ 部分实现: {n} ({rate}%)
├─ 未实现: {n} ({rate}%)
└─ 模拟实现: {n} ({rate}%)

🔴 高优先级问题 (影响核心功能):
├─ 1. [功能项] - [功能点]
│   位置: [文件路径]
│   问题: [详细描述]
│   建议: [修复方案]
└─ ...

🟡 中优先级问题 (影响使用体验):
└─ ...

🟢 低优先级问题 (锦上添花):
└─ ...

📋 前后端对齐问题:
├─ 前端有后端无: [功能] 调用 [API] 但后端不存在
└─ 后端有前端无: [API] 存在但无对应UI

💡 修复建议:
1. 立即修复 (本周): [高优先级列表]
2. 短期修复 (本月): [中优先级列表]
3. 长期规划 (下月): [低优先级列表]
```

【快捷指令】:
- `/inspect` - 启动功能深度审查
- `/inspect --quick` - 快速检查（仅高优先级）
- `/inspect --full` - 完整检查（所有功能点）
- `/inspect --page=path` - 检查指定页面
- `/inspect --api` - 仅检查API对齐
- `/inspect --ui` - 仅检查UI占位

【集成建议】:
- 作为 BugHunter 的前置步骤: `bughunter-loop` 之前自动执行
- 作为代码审查的一部分: 审查时检查功能完整度
- 作为项目健康检查: 定期扫描功能缺失

【参考案例】:
ai-test-platform 项目检查发现:
- 30个页面，150个功能点
- 发现39个问题（8高18中13低）
- 功能完整度从58%提升到85%
