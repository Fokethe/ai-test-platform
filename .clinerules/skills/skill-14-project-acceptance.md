# encoding: utf-8
# -*- coding: utf-8 -*-

## Skill 14: 第三方验收专家 (Project Acceptance Expert) V5.2 - 严格模式

【触发方式】: 
- 快捷指令: `/acceptance` (必须加斜杠)
- 用户输入: "验收项目"、"版本验收"、"项目验收"
- 自动触发: 大版本发布前、重大功能上线前

【核心定位】: 
**全自动第三方独立验收专家**，采用 **11维度 × 五阶段矩阵** 对项目进行深度验收。
**关键特性**: 验收流程完全自动化，严格模式 (100%检查点覆盖+99分+99%修复率)，强制完整执行56检查点。

## 【基础工具引用】

本 Skill 使用 Skill 00 提供的基础工具：
- 路径验证器: 自动修正文件路径
- 编码修复器: 自动设置 UTF-8
- 检查点追踪器: 追踪44+12=56个检查点
- 同步检查清单: 确保更新同步

详细定义: `.clinerules/skills/skill-00-global-tools.md`

---

## 【⚠️ 执行前强制指令 - 严格模式初始化】

**当用户触发 `/acceptance` 或"验收项目"时，Cline 必须立即执行以下操作：**

### 1. 立即执行 /yolo 进入自动模式
```
🤖 验收专家已激活 - 进入严格模式
├── 模式: STRICT (100%通过标准)
├── 行为: 强制完整执行56检查点，不允许跳过
├── 通过标准: 必须100%通过，不允许低于阈值
├── 中断策略: 遇到任何失败立即停止，必须修复后才能继续
└── 路径验证: 所有文件操作前验证路径
```
**立即执行**: 输入 `/yolo` 进入自动执行模式，后续所有操作自动执行，不询问确认

### 2. 强制设置 UTF-8 编码 (自动执行)
```bash
# Windows: 设置UTF-8编码，解决终端乱码
chcp 65001
set LANG=zh_CN.UTF-8
```

### 3. 路径验证器初始化 (自动执行)
```typescript
// 所有文件操作必须通过路径验证器
function validatePath(filePath: string): string {
  const PROJECT_ROOT = 'ai-test-platform';
  
  // 检查路径是否包含项目根目录
  if (!filePath.includes(PROJECT_ROOT)) {
    // 自动修正路径
    return `ai-test-platform/${filePath}`;
  }
  
  return filePath;
}
```

### 4. 检查点追踪器初始化
```typescript
interface CheckpointTracker {
  total: 56;                    // 总检查点数 (44+12)
  completed: number;            // 已完成
  failed: number;               // 失败
  skipped: number;              // 跳过 (严格模式下必须为0)
  status: Map<string, boolean>; // 每个检查点状态
}

// 严格模式: 不允许任何检查点被跳过
const STRICT_MODE = {
  allowSkip: false,
  passThreshold: 100,  // 100%
  requireAllPass: true,
  stopOnFirstFailure: true
};
```

---

## 【严格模式通过标准 V5.2】

```
✅ 强制满足 (缺一不可):
├── 检查点覆盖率 = 100% (56/56)
├── 维度得分 ≥ 99分  ⚠️  STRICT: <99分不准过
├── 问题修复率 ≥ 99%
├── 高优先级漏洞 = 0
├── 中优先级漏洞 = 0
├── 低优先级漏洞 = 0
├── 11维度 × 5阶段 检查点通过率 = 100%
├── 44基础检查点 + 12运行时检查点 = 56检查点全部通过
├── 关键维度 (功能/性能/安全) 得分 ≥ 99分
├── 测试覆盖率 = 100%
├── 无跳过检查点
├── 运行时错误 = 0
├── API平均响应 < 3s
├── 页面加载时间 < 3s
├── 控制台错误 = 0
└── 根目录外溢文件 = 0

❌ 严格模式禁止:
├── 跳过任何检查点
├── 低于100%检查点覆盖率
├── 维度得分 < 99分 (核心红线)
├── 问题修复率 < 99%
├── 未发现的问题进入下一阶段
├── 提前结束验收流程
└── 未修复问题就标记完成
```

---

## 【11×5验收矩阵】

每个维度 × 每个阶段 = 56个检查点

```
维度/阶段    阶段1:静态分析    阶段2:动态验证    阶段3:数据流验证    阶段4:端到端测试    阶段5:运行时验证
────────────────────────────────────────────────────────────────────────────────────────────────
1.功能验收      代码规范检查      功能可用性测试    数据结构对齐      业务闭环验证       所有页面参数验证
               (代码质量)       (页面/API可访问)   (前后端一致)      (完整用户流程)     (所有68页面)

2.性能验收      配置检查         响应时间测试      性能指标监控      压力/负载测试      实际用户操作响应
               (优化配置)       (实际响应)        (数据指标)        (极限场景)         (<1s交互响应)

3.安全验收      代码安全扫描      漏洞动态检测      权限验证         渗透测试          运行时安全验证
               (静态分析)       (运行时检测)      (数据权限)        (模拟攻击)         (XSS/CSRF/敏感信息)

4.可移植性      Dockerfile检查    容器运行测试      配置外部化验证    跨环境部署测试      多环境运行时验证
               (镜像规范)       (容器启动)        (环境变量)        (多环境验证)       (浏览器/移动端/分辨率)

5.可维护性      代码复杂度分析    重构建议验证      依赖关系检查      文档完整性验证      运行时错误监控
               (圈复杂度)       (代码异味)        (耦合度)          (维护文档)         (异常处理/错误边界)

6.兼容性        浏览器配置检查    多端适配测试      API版本兼容性    降级策略验证        API运行时兼容
               (browserslist)   (响应式/移动端)   (接口兼容)        (graceful降级)     (版本/数据格式/降级)

7.可用性        UI组件检查       交互流程测试      状态管理验证      用户操作流程        用户操作流程验证
               (组件规范)       (点击/输入)       (状态一致性)      (端到端体验)       (完整工作流/错误提示)

8.可靠性        异常代码检测      容错机制测试      恢复机制验证      故障演练测试        容错和恢复验证
               (错误处理)       (异常捕获)        (数据恢复)        (故障注入)         (网络中断/服务端错误)

9.用户文档集    文件存在性检查    内容准确性验证    示例可运行性      文档时效性检查      运行时文档验证
               (文件齐全)       (内容正确)        (代码可执行)      (更新及时)         (帮助提示/示例/链接)

10.产品说明     需求追溯检查      功能覆盖度验证    验收标准对齐      用户确认签字        功能符合需求
               (需求映射)       (功能完整)        (标准符合)        (用户验收)         (所有功能/PRD/需求)

11.工作流完整   代码逻辑检查      流程可执行性      数据流转验证      角色权限验证        端到端流程验证
               (逻辑正确)       (步骤可执行)      (数据完整性)      (权限矩阵)         (创建/执行/查看报告)
```

---

## 【/yolo 和 /compact 实际执行定义】

### /yolo 指令 - 自动执行模式 (实际执行)

```yaml
指令: /yolo
触发: 验收开始时**必须第一个执行**
执行动作:
  1. 设置 session.autoMode = true
  2. 设置 session.confirmationRequired = false
  3. 输出: "🤖 已进入YOLO模式 - 自动执行，无需确认"
  4. 后续所有操作自动执行，不询问用户确认

自动执行效果:
  - 命令自动执行: chcp 65001 >nul 2>&1 && command
  - 文件自动创建/修改 (路径自动修正)
  - 测试自动运行
  - 仅关键节点输出结果
  - 遇到错误时暂停并报告
```

### /compact 指令 - 上下文压缩 (实际执行)

```yaml
指令: /compact
触发方式:
  - 每完成一个大阶段后**自动执行** (推荐)
  - 上下文使用率 > 60% (自动触发)
  - 进入修复阶段前 (强制触发)

实际执行动作:
  1. 💾 保存当前状态到 memory.md
  2. 🗜️  压缩上下文
  3. 📊 显示: "🗜️ Compact完成 - 使用率: {before}% → {after}%"
  4. ▶️  继续执行任务
```

---

## 【🚀 全自动验收执行流程 V5.2 - 严格模式】

**⚠️ 核心原则: 强制完整执行，100%通过，不跳过任何检查点，<99分不准过**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 0: 初始化 (自动执行)                                                    │
│ ├── 立即执行 /yolo (第一个操作)                                              │
│ ├── 设置 UTF-8 编码 (chcp 65001)                                           │
│ ├── 初始化路径验证器                                                        │
│ ├── 初始化检查点追踪器 (56个检查点清单)                                       │
│ ├── 生成 batchId: acceptance-YYYYMMDD-XXX                                  │
│ └── 创建验收报告目录                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 【发现阶段】Step 1-5: 完整扫描56检查点 (只发现，不修复)                         │
│ ⚠️ 重要规则: 只记录问题，绝不修复                                            │
│                                                                             │
│ Step 1-4: 原44检查点 (静态/动态/数据流/端到端)                                │
│ Step 5: 新增12运行时检查点 (全覆盖所有68页面)                                 │
│                                                                             │
│ 每完成一个大阶段后:                                                         │
│ ├── 自动检查上下文使用率                                                    │
│ ├── 如果 > 60%: 自动执行 /compact                                            │
│ └── 继续下一阶段                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ├── ✅ 56/56 检查点全部通过 → 跳到 Step 8 (生成最终报告)
    │
    └── ❌ 任何检查点失败 → 进入 Step 6-7 (强制修复循环)
         │
         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 【强制修复】Step 6-7: 必须100%修复后才能继续                                   │
│                                                                             │
│ Step 6: 并行修复 (SubAgent)  ⚠️ 必须使用并行修复                              │
│ ├── 使用 use_subagents 启动最多5个并行 SubAgent                              │
│ ├── 每个SubAgent执行TDD修复循环                                               │
│ └── 🔴→🟢→🔧→✅                                                            │
│                                                                             │
│ Step 7: 强制回归测试                                                          │
│ ├── 重新执行所有失败的检查点                                                   │
│ ├── ❌ 仍有失败 → 继续修复循环 (无最大迭代限制)                                  │
│ └── ✅ 全部通过 → 继续                                                       │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 8: 生成最终验收报告 (单份报告)                                           │
│ ├── 验证: 56/56 检查点全部通过                                                 │
│ ├── 验证: 维度得分 ≥ 99分  ⚠️  STRICT红线                                     │
│ ├── 验证: 无跳过检查点                                                        │
│ ├── 输出: 简洁美观的验收报告 (report.md + report.html)                        │
│ └── 直接在报告上追加更新，不多余阐述                                          │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 9: 自动项目清理 (Skill 11)  ⚠️ 不可删除                                  │
│ ├── 自动触发 `/cleanup`                                                      │
│ ├── 清理临时文件、测试缓存、备份文件                                          │
│ ├── 整理报告目录结构                                                         │
│ └── 确保根目录无外溢文件                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【报告格式规范】

### 单份报告原则
- **只生成一份报告**: `report.md` + `report.html`
- **直接在原有报告上追加更新**，不生成 report-final.md 等多余文件
- **简洁美观**，不重复阐述"修复了啥"

### 报告结构
```markdown
# AI Test Platform - 验收报告

## 验收概览
| 指标 | 数值 |
|------|------|
| 检查点覆盖 | 56/56 (100%) |
| 维度得分 | XX/100 |
| 测试通过率 | XXX/XXX (XX%) |

## 各维度得分
| 维度 | 得分 | 状态 |
|------|------|------|
| ... | ... | ... |

## 问题汇总
### 高优先级 (P0)
- [ ] 问题1...

### 中优先级 (P1)
- [ ] 问题2...

## 验收结论
- 状态: 通过 / 有条件通过 / 不通过
- 建议: ...
```

---

## 【🎮 快捷指令 V5.2】

- `/acceptance` - 启动严格模式完整验收 (100%+99+99%标准，56检查点)
- `/acceptance --strict` - 强制严格模式 (默认)
- `/acceptance --parallel` - 使用 SubAgent 并行验收
- `/acceptance --report` - 查看验收报告
- `/yolo` - 进入自动执行模式（**验收开始时第一个执行**）
- `/compact` - 执行上下文压缩（**大阶段后自动执行**）
- `/cost` - 显示当前消耗

---

## 【⚠️ Skill修改强制检查清单 V5.2】

**修改本Skill时，必须完成以下同步更新：**

```markdown
- [ ] 1. 更新 `.clinerules` (触发关键词/快捷指令，如有变更)
- [ ] 2. 更新 `.clinerules/skills/skill-14-project-acceptance.md` (本文件)
- [ ] 3. 更新 `.kimi/skills/project-acceptance/skill-14-optimized.md` (Kimi版本)
- [ ] 4. 更新 `.clinerules/USAGE.md` (快捷指令表)
- [ ] 5. 更新 `.kimi/skills/USAGE_GUIDE.md` (Kimi使用指南) ⭐ 必须更新
- [ ] 6. 更新版本号 (5.1 → 5.2)
- [ ] 7. 更新最后更新时间
- [ ] 8. 验证 Cline 和 Kimi 版本内容一致
```

**违规后果**: Skill定义不一致，导致Cline和Kimi行为差异

================================================================================
*Skill版本: 5.2 | 最后更新: 2026-03-12 | 核心增强: /yolo开头 + /compact大阶段后 + Step9自动清理 + 严格99分红线 + 单份报告*
