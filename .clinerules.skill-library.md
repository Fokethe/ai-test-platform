# encoding: utf-8
# -*- coding: utf-8 -*-

# Cline 智能助手 Skill 备用库
# 版本: 2.0 | 包含 18 个扩展 Skill
# 
# 使用方式:
# 1. 按需加载: "从备用库加载 Skill XX"
# 2. 查看清单: "显示所有可用 Skill"
# 3. 切换完整模式: "切换到完整 Skill 模式"

================================================================================
## Skill 11: 视觉还原协议 (Visual Coding)
================================================================================

【触发方式】: 用户上传图片/截图后说"还原这个界面"、"实现这个设计"、"截图转代码"

【执行流程】:
1. **结构分析**: 先输出"这是什么页面，有哪些模块"
2. **设计规范提取**（四维度专业分析）:
   - **字体**: 识别字体家族、字重、行高、字间距
   - **颜色**: 提取主色、辅助色、背景色、文字色（禁止默认渐变色）
   - **动效**: 识别过渡时间、缓动函数、动画类型
   - **背景**: 分析背景类型（纯色/渐变/图片/毛玻璃）
3. **像素级还原**:
   - 配色: 精确匹配截图中的颜色，使用 Tailwind 标准色板
   - 间距: 使用 Tailwind 标准间距 scale (4px 基准)
   - 字体: 匹配字体大小和字重，使用标准字体栈
   - 圆角: 匹配 border-radius，使用标准 scale
4. **布局实现**:
   - 使用 Flex/Grid
   - 禁止绝对定位（除非是悬浮按钮）
5. **响应式推导**:
   - 如果是桌面端截图，自动推导移动端布局
   - 说明折叠/堆叠策略
6. **交互状态**:
   - 所有按钮添加 hover/active/focus 状态
   - 添加适当的过渡动画
7. **组件化**:
   - 拆分为 Header/Content/Sidebar 等逻辑组件

【设计规范标准】: 见精简版 Skill 11 完整定义

================================================================================
## Skill 12: TDD 循环开发 (TDD Loop)
================================================================================

【触发方式】: "TDD 模式"、"循环开发"、"迭代开发"、"先写测试"、"红绿重构"

【核心循环】:
```
红阶段: 写测试 → 运行（应失败）
绿阶段: 最小实现 → 运行（应通过）
重构: 优化代码 → 运行（仍通过）
下一轮: 询问是否继续
```

【执行流程】:
1. **需求分析**: 分析功能点，预估测试用例数
2. **红阶段**: 仅编写测试代码，不实现功能
3. **绿阶段**: 编写最小代码使测试通过
4. **自动修复**: 测试失败时自动尝试修复（最多3次）
5. **重构**: 改进代码质量，保持测试通过
6. **迭代决策**: 显示完成状态，询问下一步

【快捷指令】:
- `/tdd` - 进入 TDD 模式
- `/tdd next` - 当前功能完成，进入下一个功能
- `/tdd fix` - 手动触发修复当前失败测试
- `/tdd stop` - 停止 TDD 循环

================================================================================
## Skill 13: 代码重构协议 (Code Refactor)
================================================================================

【触发方式】: "重构"、"优化代码"、"重构 {文件名}"、"整理项目结构"

【执行策略】:
1. **依赖分析**: 使用 Read/Glob 分析文件关系，识别底层工具 → 业务逻辑 → UI 层
2. **分批修改**: 每次最多处理 3 个紧密关联的文件
3. **安全原则**:
   - 保持函数签名向后兼容
   - 修改前输出 diff 对比
   - 建议用户先 `git add .` 再应用修改
4. **输出格式**:
```
🔧 重构计划: {模块名}
  ├── 文件1: {文件名} - 修改点: {简述}
  ├── 文件2: {文件名} - 修改点: {简述}
  └── 依赖检查: {是否影响其他文件}

[diff 对比]
```

【约束】:
- 禁止自动执行 rm/drop 等危险命令
- 修改后必须询问 "是否应用这些修改?"

================================================================================
## Skill 14: 持久化规划协议 (Planning with Files)
================================================================================

【触发方式】: "创建任务计划"、"规划项目"、"复杂任务"、"多步骤任务"、"/plan"

【核心思想】: 复刻 Manus 的持久化规划，通过 Markdown 文件解决上下文漂移问题

【执行流程】:
1. **初始化规划文档**:
   创建 `task_plan.md`（任务计划）包含: 目标、执行步骤、当前状态、依赖关系

2. **执行与记录**:
   创建 `findings.md`（发现记录）包含: 关键发现、技术决策、遇到的问题

3. **进度追踪**:
   更新 `progress.md`（进度追踪）包含: 已完成、进行中、阻塞项

【工作流程】:
1. 用户提出复杂任务 → 创建 task_plan.md
2. 每完成一步 → 更新 task_plan.md 和 progress.md
3. 遇到重要发现 → 记录到 findings.md
4. 会话中断后恢复 → 读取三个文件，继续执行

================================================================================
## Skill 15: 上下文管理协议 (Context Management)
================================================================================

【触发方式】: "总结进度"、"更新 progress"、"防止失忆"、"/compact"

【管理策略】:
1. **每 10-15 轮对话**: 说"总结当前进度，更新 progress.md"
2. **切换功能时**: 新建会话，粘贴 KIMI.md + progress.md 作为开场
3. **代码过长时**: 说"只展示修改的部分，用 diff 格式"

【会话开场标准模板】:
```
读取 KIMI.md 和 progress.md。

今日目标: [具体功能]

开发前确认:
1. 这个功能依赖哪些已完成的部分？
2. 是否需要新建文件？路径是什么？
3. 潜在的技术难点是什么？

确认后，开始第一步: [具体步骤]
```

【快捷指令】: `/compact` - 压缩上下文

================================================================================
## Skill 16: Skill 创建器 (Skill Creator)
================================================================================

【触发方式】: "创建新 skill"、"添加 skill"、"/skill:create"、"自定义 skill"

【执行流程】:
1. **需求收集**: 询问 Skill 名称、触发方式、核心功能、输入输出
2. **生成 Skill 定义**: 按照标准模板创建 Skill
3. **集成到 .clinerules**: 添加到 Skill 编排器的意图识别表
4. **验证测试**: 生成测试用例，验证触发词是否有效

【Skill 设计原则】:
- 单一职责: 一个 Skill 只做一件事
- 明确触发: 清晰的触发条件和关键词
- 可重复: 相同输入产生一致输出
- 可验证: 有明确的完成标准

================================================================================
## Skill 17: Git 提交规范 (Commit Convention)
================================================================================

【触发方式】: "生成提交信息"、"写 commit"、"commit message"、"这次改动怎么提交"

【格式模板】:
```
<type>(<scope>): <subject>

<body>

<footer>
```

【Type 定义】:
- feat: 新功能
- fix: Bug 修复
- docs: 文档更新
- style: 代码格式 (不影响功能)
- refactor: 重构
- test: 测试相关
- chore: 构建/工具变动

【规则】:
1. Subject ≤ 50 字符，使用祈使句 ("添加" 而非 "添加了")
2. Body 说明改动原因 (why)，每行 ≤ 72 字符
3. Footer: 破坏性变更标注 BREAKING CHANGE: {描述}

================================================================================
## Skill 18: 项目知识库上下文 (Project Context)
================================================================================

【触发方式】: "根据规范"、"按项目风格"、"文档里说的"、需要了解项目结构时

【项目结构】:
- docs/: 文档目录 (PRD, 开发指南, 测试规范, Prompt库)
- src/: 源码，按功能域组织
- prisma/: 数据库模型和迁移

【技术栈约束】:
- 框架: Next.js 16 + React 19 + TypeScript
- 样式: Tailwind CSS v4 + shadcn/ui 组件
- 数据库: Prisma ORM + SQLite(开发)/PostgreSQL(生产)
- 认证: NextAuth.js v4

【强制规范】:
- Mobile-first: 默认移动端，断点 sm: md: lg:
- 组件: 函数式 + Hooks，禁止 Class 组件
- 类型: 禁止 any，所有 API 返回定义 interface

================================================================================
## 快捷指令汇总
================================================================================

| 指令 | 功能 | 所属 Skill |
|------|------|-----------|
| `/cost` | 显示当前累计消耗 | Cost Control |
| `/compact` | 压缩上下文 | Context Management |
| `/next` | 继续下一阶段/下一块 | Task Planner / Doc Processor |
| `/plan` | 重新生成计划 | Task Planner |
| `/parallel` | 启动多任务调度 | Multi-Task Scheduler |
| `/bughunter` | 启动深度修复 | BugHunter |
| `/tdd` | 进入 TDD 模式 | TDD Loop |
| `/tdd next` | 下一轮 TDD | TDD Loop |
| `/tdd stop` | 停止 TDD 循环 | TDD Loop |

================================================================================
## 使用说明
================================================================================

**加载方式**:
1. "从备用库加载 Skill 11" → 加载视觉还原协议
2. "从备用库加载 TDD 开发" → 加载 TDD 循环开发
3. "显示所有可用 Skill" → 列出本文件所有 Skill 清单
4. "切换到完整 Skill 模式" → AI 读取本文件作为完整配置

**建议**:
- 日常开发使用精简版 (10个核心 Skill)
- 特殊需求时按需加载本库中的 Skill
- 如需频繁使用某个 Skill，考虑将其加入精简版

================================================================================
*配置版本: 2.0 | 最后更新: 2026-02-28 | 备用 Skill: 18个*
