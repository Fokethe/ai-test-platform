# Kimi Code Skills 使用指南

## 快速调用命令

### 基础调用方式

```bash
# 调用单个 Skill
/skill:skill-name

# 示例
/skill:cost-control
/skill:code-review
/skill:task-planner
```

### 快捷指令

| 指令 | 所属 Skill | 功能 |
|------|-----------|------|
| `/cost` | cost-control | 显示当前累计消耗 |
| `/compact` | context-management | 压缩上下文 |
| `/next` | doc-processor / task-planner | 继续下一块/下一阶段 |
| `/plan` | doc-system / task-planner | 重新生成计划或文档 |

---

## Skill 触发关键词速查

无需记忆命令，说出以下关键词即可自动触发：

| 你想做什么 | 说这些关键词 | 自动触发 Skill |
|-----------|-------------|---------------|
| 查看消耗 | "查一下用了多少token" | cost-control |
| 处理大文档 | "整理这个文档" "文件太大了" | doc-processor |
| 重构代码 | "重构这个文件" "优化代码" | code-refactor |
| 规划任务 | "帮我做个计划" "Plan Mode" | task-planner |
| 审查代码 | "看看这代码有问题吗" "review" | code-review |
| 写提交信息 | "怎么提交" "写commit" | git-commit |
| 了解项目规范 | "按项目规范" "文档里说的" | project-context |
| 澄清需求 | "我要做个新项目" "需求不太清楚" | socratic-inquiry |
| 初始化文档 | "创建项目文档" "初始化" | doc-system |
| 开始开发 | "开始开发" "进入工作流" | workflow |
| 还原设计稿 | "实现这个设计" "截图转代码" | visual-coding |
| 修复错误 | "报错了" "修复这个错误" | debug-diagnosis |
| 总结进度 | "总结下进度" "防止失忆" | context-management |

---

## 💡 高级使用技巧（来自实战）

### 1. Plan Mode 工作流（复杂任务必备）

**核心原则：先规划，后执行，方案留底**

```
Step 1: 进入 Plan Mode
        ↓ 说"进入 Plan Mode" 或 "先规划再执行"
        AI只分析不修改，输出详细计划

Step 2: 保存计划
        ↓ 说"先把计划保存到 plan.md"
        方案留底，后续可回溯

Step 3: Review 计划（可选）
        ↓ 说"扮演 Staff Engineer 审查这个计划"
        第二视角检查方案可行性

Step 4: 执行计划
        ↓ 确认后说"开始执行"
        按 plan.md 逐步实施
```

**何时用 Plan Mode**：
- 接手新项目 → 出架构图 + 入口分析
- 做大功能 → 模块拆分、接口设计
- 重构优化 → 分析问题，给出方案
- **进展跑偏时** → 立即切回 Plan Mode 重新规划

---

### 2. 提升 Prompt 质量的话术

| 目的 | Prompt 话术 |
|------|------------|
| **角色反转** | "针对这些改动向我提问，在我通过你的测试之前不要提交" |
| **要求自证** | "证明这套方案行得通，对比修改前后的差异" |
| **推倒重来** | "基于你现在掌握的信息，推翻刚才的方案，换一个更优雅的实现" |
| **先写 Spec** | "先写详细的规格说明 Spec，确认后再动手实现" |
| **解释模式** | "边改边解释为什么这么改，开启解释模式" |
| **架构可视化** | "给我画个 ASCII 流程图，解释模块调用链" |
| **费曼学习** | "我解释给你听，你通过提问填补我的知识盲区" |

---

### 3. 成本控制最佳实践

| 策略 | 操作 |
|------|------|
| **长任务定期压缩** | 每 10-15 轮说 `/compact`，别等报错才后悔 |
| **切模型省成本** | 简单任务（格式化、注释）明确说"用轻量模式" |
| **监控消耗** | 长对话必说 `/cost`，避免超额 |
| **大文件分段** | 文档 > 30,000 字符自动触发分段处理 |

---

## 场景组合使用指南

### 场景 1：从零开始新项目

**完整流程**（含成本控制 + 文档更新）：

```
Step 1: /skill:cost-control
        ↓ 说"/cost"查看初始状态
        建立成本基线

Step 2: /skill:socratic-inquiry
        ↓ 说"我要做个新项目，进入 Plan Mode"
        苏格拉底式审问需求

Step 3: /skill:task-planner
        ↓ 说"制定开发计划，保存到 plan.md"
        拆解为可执行任务，方案留底
        【自动更新 doc-system: plan.md】

Step 4: /skill:doc-system
        ↓ 说"创建 KIMI.md 和 progress.txt"
        初始化项目双文档
        【更新文档: KIMI.md, progress.txt】

Step 5: /skill:cost-control
        ↓ 每阶段后说"/cost"
        监控累计消耗

Step 6: /skill:workflow
        ↓ 说"按 plan.md 开始开发"
        按四步工作流执行
        【每完成一步更新 progress.txt】
```

**文档产出**：
- `plan.md` - 完整开发计划
- `KIMI.md` - AI 操作手册
- `progress.txt` - 进度追踪

---

### 场景 2：日常功能开发

**标准流程**（强制成本控制 + 文档同步）：

```
Step 1: 开场自动读取
        ↓ /skill:context-management 自动执行
        读取 KIMI.md + progress.txt + plan.md

Step 2: /skill:cost-control
        ↓ 说"/cost"查看当前消耗
        确认成本预算

Step 3: /skill:task-planner
        ↓ 描述功能需求，说"进入 Plan Mode"
        生成执行计划
        【更新文档: 追加到 plan.md】

Step 4: /skill:doc-processor（如有大文档）
        ↓ 引用文档 > 30,000 字符时自动触发
        分段处理需求文档
        【成本控制: 显示分段消耗预估】

Step 5: /skill:workflow
        ↓ 按计划逐步实现
        【每完成一个功能更新 progress.txt】
        【每 3-5 步检查 /cost】

Step 6: /skill:code-review
        ↓ 说"review下代码，开启解释模式"
        代码审查 + 学习输出

Step 7: /skill:doc-system
        ↓ 说"更新 progress.txt"
        标记完成功能
        【更新文档: progress.txt】

Step 8: /skill:git-commit
        ↓ 说"生成提交信息"
        生成 commit message

Step 9: /skill:cost-control
        ↓ 说"/cost"查看本轮总消耗
        成本总结
```

**快捷模式**：
> "我要添加用户登录功能，进入 Plan Mode，制定计划后保存到 plan.md，然后按计划开发，每步更新 progress.txt，最后 review 代码"

---

### 场景 3：代码重构优化

**安全重构流程**（含回滚保护）：

```
Step 1: /skill:cost-control
        ↓ 说"/cost"建立基线
        预估重构成本

Step 2: /skill:code-review
        ↓ 说"review这个模块，画 ASCII 架构图"
        识别问题点 + 可视化理解

Step 3: /skill:task-planner
        ↓ 说"制定重构计划，进入 Plan Mode"
        输出重构方案
        【更新文档: 保存重构计划到 refactor-plan.md】

Step 4: /skill:code-refactor
        ↓ 说"按 refactor-plan.md 执行，先展示 diff"
        分批重构（每次最多 3 文件）
        【每批后说"/cost"监控消耗】

Step 5: /skill:debug-diagnosis（如出错）
        ↓ 说"验证重构是否引入 bug"
        自证方案可行性

Step 6: /skill:code-review
        ↓ 说"review重构后的代码，要求自证"
        验证重构质量

Step 7: /skill:doc-system
        ↓ 说"更新 progress.txt 和 KIMI.md"
        记录重构完成 + 技术债务清理
        【更新文档: progress.txt, KIMI.md】

Step 8: /skill:git-commit
        ↓ 说"生成提交信息，type选 refactor"
        生成 commit message

Step 9: /skill:danger-signals
        ↓ 自动监控
        确认无危险信号（如修复 3 次未好需回滚）

Step 10: /skill:cost-control
         ↓ 说"/cost"对比前后消耗
         重构成本总结
```

---

### 场景 4：处理大型文档/规范

**强制分段处理流程**：

```
Step 1: /skill:cost-control
        ↓ 自动触发
        "📊 读取此文档预估消耗: ${cost} (~{n} tokens)"

Step 2: /skill:doc-processor
        ↓ 文档 > 30,000 字符时自动触发
        "检测到大型文档，触发分段协议"
        输出文档地图 + 分块方案

Step 3: 逐块处理（成本控制嵌入）
        ↓ 块1: 说"/next"
        【显示】"处理块 1/5，预估消耗: ${cost}"
        处理内容...
        【更新文档: 输出块1-summary.md】
        
        ↓ 块2: 说"/next"
        【显示】"处理块 2/5，累计消耗: ${cost}"
        处理内容...
        【更新文档: 输出块2-summary.md】
        
        ...（每块都显示成本）

Step 4: 累积检查
        ↓ 单轮 > $0.05 时
        【强制询问】"预估消耗 ${cost}，是否继续? [Y/N]"

Step 5: /skill:doc-processor
        ↓ 全部块完成后
        整合所有块，去重，输出最终文档
        【更新文档: final-document.md】

Step 6: /skill:cost-control
        ↓ 说"/cost"
        总消耗统计
```

---

### 场景 5：UI 开发（设计稿还原）

**像素级还原流程**：

```
Step 1: /skill:cost-control
        ↓ 说"/cost"建立基线
        预估还原成本

Step 2: /skill:visual-coding
        ↓ 上传截图 + 说"还原这个设计，进入 Plan Mode"
        结构分析 + 规范提取
        【更新文档: 保存 design-spec.md（配色/间距/字体规范）】

Step 3: /skill:task-planner（复杂界面）
        ↓ 说"制定组件拆分计划"
        拆分为 Header/Content/Sidebar 等
        【更新文档: 追加组件清单到 plan.md】

Step 4: 分组件实现
        ↓ 组件1: Header
        输出代码...
        【更新文档: 代码保存到 components/Header.tsx】
        【说"/cost"检查消耗】
        
        ↓ 组件2: Content
        输出代码...
        【更新文档: 代码保存到 components/Content.tsx】
        【说"/cost"检查消耗】

Step 5: /skill:code-review
        ↓ 说"review所有组件，检查响应式"
        代码审查

Step 6: /skill:visual-coding
        ↓ 说"对比设计稿，检查还原度"
        像素级对比验证

Step 7: /skill:doc-system
        ↓ 说"更新 progress.txt，标记 UI 完成"
        记录完成状态
        【更新文档: progress.txt, KIMI.md（添加设计规范）】

Step 8: /skill:git-commit
        ↓ 说"生成提交信息，type选 feat"
        生成 commit message

Step 9: /skill:cost-control
        ↓ 说"/cost"
        UI 开发成本总结
```

---

### 场景 6：Debug 调试

**问题修复流程**（防循环保护）：

```
Step 1: /skill:cost-control
        ↓ 说"/cost"建立基线
        记录修复前消耗

Step 2: /skill:debug-diagnosis
        ↓ 粘贴错误 + 说"分析原因，进入解释模式"
        根因分析 + 修复方案
        【更新文档: 保存 debug-analysis.md】

Step 3: 选择修复方案
        ↓ AI生成修复代码
        【显示 diff 对比】

Step 4: /skill:danger-signals
        ↓ 自动监控
        检查是否第 3 次修复（危险信号）
        【如 3 次未好 → 提示回滚到稳定版本】

Step 5: /skill:debug-diagnosis
        ↓ 说"验证修复，要求自证"
        证明方案可行

Step 6: /skill:code-review
        ↓ 说"review修复后的代码"
        验证修复质量

Step 7: /skill:doc-system
        ↓ 说"更新 progress.txt，记录修复"
        记录问题 + 解决方案
        【更新文档: progress.txt（技术债务区）】

Step 8: /skill:git-commit
        ↓ 说"生成提交信息，type选 fix"
        生成 commit message

Step 9: /skill:cost-control
        ↓ 说"/cost"
        Debug 成本统计
```

**防循环规则**：修复 3 次仍未解决 → `danger-signals` 强制提示回滚。

---

### 场景 7：长会话维护（防失忆）

**定期维护流程**：

```
每 5-8 轮对话（或上下文 > 40%）:
├── /skill:cost-control
│   ↓ 说"/cost"
│   显示当前消耗统计
│
├── /skill:context-management
│   ↓ 说"/compact"
│   压缩上下文，保留关键信息
│   【显示】"💡 上下文即将满载，建议压缩"
│
└── 每 10-15 轮:
    └── /skill:doc-system
        ↓ 说"总结进度，更新 progress.txt"
        输出状态摘要 + 更新文档
        【更新文档: progress.txt, KIMI.md（当前目标）】

上下文 > 60% 时:
├── /skill:danger-signals
│   ↓ 自动触发
│   🚨 "上下文占用 > 60%，建议开新对话"
│
└── /skill:context-management
    ↓ 选择:
    - 说"/compact" 继续压缩
    - 或新开会话 + 粘贴 KIMI.md + progress.txt
```

---

### 场景 8：代码提交前检查

**完整检查清单**（强制成本核算）：

```
Step 1: /skill:cost-control
        ↓ 说"/cost"记录当前消耗
        建立检查基线

Step 2: /skill:code-review
        ↓ 说"review要提交的代码，开启解释模式"
        全面检查（可读性/性能/安全/类型）
        【输出】检查清单结果

Step 3: /skill:project-context
        ↓ 说"检查是否符合项目规范"
        验证规范符合度（KIMI.md 技术铁律）

Step 4: /skill:debug-diagnosis
        ↓ 说"自证这套改动不会引入 bug"
        要求证明方案可行性

Step 5: /skill:doc-system
        ↓ 说"更新 progress.txt，标记待提交"
        记录待提交状态
        【更新文档: progress.txt】

Step 6: /skill:git-commit
        ↓ 说"生成提交信息"
        生成标准 commit message

Step 7: /skill:cost-control
        ↓ 说"/cost"
        本次开发总消耗统计
```

---

## 常用组合速记

| 你想做什么 | 输入命令序列 |
|-----------|-------------|
| **开始新项目** | `/cost` → `socratic-inquiry` → `task-planner`（保存plan.md）→ `doc-system` → `workflow` |
| **开发新功能** | `/cost` → `task-planner`（Plan Mode）→ `doc-processor`（如有大文档）→ `workflow` → `code-review` → `doc-system`（更新progress）→ `/cost` |
| **重构代码** | `/cost` → `code-review`（ASCII图）→ `task-planner`（保存refactor-plan.md）→ `code-refactor` → `code-review` → `doc-system` → `git-commit` → `/cost` |
| **修复 Bug** | `/cost` → `debug-diagnosis` → `danger-signals` → `code-review` → `doc-system`（记录问题）→ `git-commit` → `/cost` |
| **还原设计稿** | `/cost` → `visual-coding`（保存design-spec.md）→ `task-planner`（组件拆分）→ 分组件实现 → `code-review` → `doc-system` → `git-commit` → `/cost` |
| **整理大文档** | `/cost` → `doc-processor` → 逐块处理（每块显示成本）→ 整合 → `/cost` |
| **提交代码** | `/cost` → `code-review` → `project-context` → `doc-system` → `git-commit` → `/cost` |
| **检查健康度** | `/cost` → `/compact` → `context-management` → `danger-signals` → `doc-system`（更新进度）→ `/cost` |

---

## 最佳实践

### 1. 强制成本意识

- **每轮大操作前**说 `/cost`
- **长任务定期** `/compact`（每 10-15 轮）
- **单轮 > $0.05** 时 AI 会强制询问是否继续
- **简单任务**明确说"用轻量模式"节省成本

### 2. 文档驱动开发

- **任何规划**先保存到 `plan.md`
- **任何完成**更新 `progress.txt`
- **任何规范**记录到 `KIMI.md`
- **大文档处理**分块保存 `block-X-summary.md`

### 3. Plan Mode 优先

复杂任务（>3步）**必须**先进入 Plan Mode：
- ❌ "帮我实现用户系统"
- ✅ "进入 Plan Mode，帮我规划用户系统实现，保存到 plan.md"

### 4. 危险信号必处理

看到 🚨 警告时**必须**确认后再继续：
- AI 重复内容 → 新开会话
- 3 次修复失败 → 回滚稳定版本
- 一次生成 5+ 文件 → 要求粒度细化

### 5. 自证与 Review

关键节点**必须**验证：
- "证明这套方案行得通"
- "扮演 Staff Engineer 审查计划"
- "对比修改前后的差异"

---

## 快速参考卡片

```
┌─────────────────────────────────────────────────────────────────┐
│  项目启动: /cost → socratic-inquiry → task-planner(save plan.md) │
│            → doc-system(init KIMI.md+progress.txt) → workflow   │
├─────────────────────────────────────────────────────────────────┤
│  功能开发: /cost → task-planner(Plan Mode) → doc-processor(大文档)│
│            → workflow → code-review → doc-system(update) → /cost │
├─────────────────────────────────────────────────────────────────┤
│  代码重构: /cost → code-review(ASCII) → task-planner(save plan)  │
│            → code-refactor → code-review → doc-system → /cost    │
├─────────────────────────────────────────────────────────────────┤
│  Debug修复: /cost → debug-diagnosis → danger-signals → code-review│
│             → doc-system(记录问题) → git-commit → /cost           │
├─────────────────────────────────────────────────────────────────┤
│  设计还原: /cost → visual-coding(save spec) → task-planner       │
│            → 分组件实现 → code-review → doc-system → /cost       │
├─────────────────────────────────────────────────────────────────┤
│  大文档:   /cost → doc-processor(分块+每块显示成本) → /next      │
│            → 整合 → /cost                                        │
├─────────────────────────────────────────────────────────────────┤
│  快捷指令: /cost  /compact  /next  /plan                         │
├─────────────────────────────────────────────────────────────────┤
│  高级话术: "Plan Mode"  "保存到 plan.md"  "Staff Engineer审查"     │
│            "要求自证"  "ASCII架构图"  "解释模式"                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 文档更新检查清单

每次交互后确认是否更新：

- [ ] **plan.md** - 计划变更时更新
- [ ] **KIMI.md** - 技术规范/当前目标变更时更新
- [ ] **progress.txt** - 完成任何功能/修复后更新
- [ ] **block-X-summary.md** - 大文档分块处理后更新
- [ ] **design-spec.md** - UI规范确定后更新
- [ ] **debug-analysis.md** - 复杂Bug修复后更新
