---
name: skill-orchestrator
description: Auto-trigger skill sequences based on user intent keywords. Analyzes user input and automatically executes predefined skill workflows without manual /skill:xxx commands.
---

# Skill Orchestrator

自动识别用户意图并触发预设的 Skill 序列，无需手动输入 `/skill:xxx` 命令。

## 触发规则表

| 用户说这些关键词 | 自动执行的 Skill 序列 |
|----------------|---------------------|
| **"开始新项目"** / **"新项目"** / **"从零开始"** | `cost-control` → `socratic-inquiry` → `task-planner` → `doc-system` → `workflow` |
| **"开发新功能"** / **"添加功能"** / **"实现功能"** | `cost-control` → `task-planner` (Plan Mode) → `doc-processor` (如有大文档) → `workflow` → `code-review` → `doc-system` → `cost-control` |
| **"重构"** / **"优化代码"** / **"代码重构"** | `cost-control` → `code-review` (ASCII图) → `task-planner` (保存refactor-plan.md) → `code-refactor` → `code-review` → `doc-system` → `git-commit` → `cost-control` |
| **"修复bug"** / **"debug"** / **"报错"** / **"出错了"** | `cost-control` → `debug-diagnosis` → `danger-signals` → `code-review` → `doc-system` → `git-commit` → `cost-control` |
| **"还原设计"** / **"设计稿"** / **"截图转代码"** / **"实现这个设计"** | `cost-control` → `visual-coding` (保存design-spec.md) → `task-planner` (组件拆分) → 分组件实现 → `code-review` → `doc-system` → `git-commit` → `cost-control` |
| **"整理文档"** / **"处理文档"** / **"文档太大"** | `cost-control` → `doc-processor` → 逐块处理（每块显示成本）→ 整合 → `cost-control` |
| **"提交代码"** / **"commit"** / **"怎么提交"** | `cost-control` → `code-review` → `project-context` → `doc-system` → `git-commit` → `cost-control` |
| **"检查健康"** / **"总结进度"** / **"防止失忆"** | `cost-control` → `context-management` → `danger-signals` → `doc-system` → `cost-control` |
| **"review"** / **"审查代码"** / **"看看代码"** | `code-review` (自动执行，无需前缀) |
| **"/cost"** / **"查消耗"** / **"用了多少"** | `cost-control` (自动执行，无需前缀) |
| **"/compact"** / **"压缩"** | `context-management` (自动执行，无需前缀) |

## 执行流程

### 1. 意图识别

当用户输入时，首先检查是否匹配触发规则表中的关键词：

```
用户输入 → 关键词匹配 → 获取 Skill 序列 → 按序执行
```

### 2. 序列执行规则

- **自动顺序执行**：按表中顺序依次调用 skills
- **条件触发**：带有 `(如有...)` 的 skill 只在满足条件时执行
- **用户确认点**：
  - Plan Mode 完成后等待用户确认
  - 每批代码修改后询问是否继续
  - 单轮预估消耗 > $0.05 时强制确认

### 3. 上下文传递

```
Skill A 执行完毕
    ↓ 传递关键信息到上下文
Skill B 读取上下文继续
    ↓ 传递关键信息到上下文
Skill C 读取上下文继续
```

## 组合详情

### 组合 A：新项目启动

**触发词**："开始新项目"、"新项目"、"从零开始"

**执行序列**：
1. `cost-control` - 建立成本基线
2. `socratic-inquiry` - 苏格拉底式审问需求
3. `task-planner` - 制定开发计划
4. `doc-system` - 初始化 KIMI.md + progress.txt
5. `workflow` - 按 plan.md 开始开发

**文档产出**：plan.md、KIMI.md、progress.txt

---

### 组合 B：功能开发

**触发词**："开发新功能"、"添加功能"、"实现功能"

**执行序列**：
1. `cost-control` - 查看当前消耗
2. `task-planner` - 进入 Plan Mode，生成执行计划
3. `doc-processor` (条件：如有大文档) - 分段处理需求文档
4. `workflow` - 按计划逐步实现
5. `code-review` - 代码审查
6. `doc-system` - 更新 progress.txt
7. `cost-control` - 本轮总消耗

**快捷说法**：
> "我要添加用户登录功能，进入 Plan Mode，制定计划后保存到 plan.md，然后按计划开发，每步更新 progress.txt，最后 review 代码"

---

### 组合 C：代码重构

**触发词**："重构"、"优化代码"、"代码重构"

**执行序列**：
1. `cost-control` - 建立基线
2. `code-review` - review模块，输出 ASCII 架构图
3. `task-planner` - 制定重构计划，保存到 refactor-plan.md
4. `code-refactor` - 分批重构（每次最多 3 文件）
5. `code-review` - 验证重构质量
6. `doc-system` - 记录重构完成
7. `git-commit` - 生成 commit message
8. `cost-control` - 重构成本总结

---

### 组合 D：Bug 修复

**触发词**："修复bug"、"debug"、"报错"、"出错了"

**执行序列**：
1. `cost-control` - 记录修复前消耗
2. `debug-diagnosis` - 根因分析 + 修复方案
3. `danger-signals` - 检查是否第 3 次修复（危险信号）
4. `code-review` - 验证修复质量
5. `doc-system` - 记录问题 + 解决方案
6. `git-commit` - 生成 commit message
7. `cost-control` - Debug 成本统计

**防循环规则**：修复 3 次仍未解决 → `danger-signals` 强制提示回滚

---

### 组合 E：设计还原

**触发词**："还原设计"、"设计稿"、"截图转代码"、"实现这个设计"

**执行序列**：
1. `cost-control` - 预估还原成本
2. `visual-coding` - 结构分析 + 规范提取，保存 design-spec.md
3. `task-planner` - 组件拆分计划
4. 分组件实现（循环执行直到完成）
5. `code-review` - 检查响应式
6. `visual-coding` - 像素级对比验证
7. `doc-system` - 记录 UI 完成
8. `git-commit` - 生成 commit message
9. `cost-control` - UI 开发成本总结

---

### 组合 F：大文档处理

**触发词**："整理文档"、"处理文档"、"文档太大"

**执行序列**：
1. `cost-control` - 显示文档读取预估消耗
2. `doc-processor` - 输出文档地图 + 分块方案
3. 逐块处理（用户说 `/next` 继续下一块）
4. 整合所有块，去重，输出最终文档
5. `cost-control` - 总消耗统计

**每块显示**：处理块 X/N，预估消耗: ${cost}

---

### 组合 G：提交代码

**触发词**："提交代码"、"commit"、"怎么提交"

**执行序列**：
1. `cost-control` - 记录当前消耗
2. `code-review` - 全面检查（可读性/性能/安全/类型）
3. `project-context` - 验证规范符合度
4. `debug-diagnosis` - 自证改动不会引入 bug
5. `doc-system` - 更新 progress.txt
6. `git-commit` - 生成标准 commit message
7. `cost-control` - 本次开发总消耗

---

### 组合 H：健康检查

**触发词**："检查健康"、"总结进度"、"防止失忆"

**执行序列**：
1. `cost-control` - 显示当前消耗统计
2. `context-management` - 压缩上下文，保留关键信息
3. `danger-signals` - 检查危险信号
4. `doc-system` - 输出状态摘要 + 更新文档
5. `cost-control` - 会话成本总结

**执行时机**：每 5-8 轮对话（或上下文 > 40%）

---

## 单 Skill 快捷触发

以下关键词直接触发单个 skill（无需等组合）：

| 关键词 | 触发 Skill |
|-------|-----------|
| "/cost"、"查消耗"、"用了多少" | `cost-control` |
| "/compact"、"压缩" | `context-management` |
| "review"、"审查代码"、"看看代码" | `code-review` |
| "规划"、"plan"、"制定计划" | `task-planner` |
| "debug"、"诊断"、"分析错误" | `debug-diagnosis` |

## 打断与跳过

用户可以在序列执行过程中：

- **说"跳过"** - 跳过当前 skill，继续下一个
- **说"停止"** - 终止整个序列
- **说"返回"** - 回到上一个 skill 重新执行
- **说"/cost"** - 立即显示当前消耗（不中断序列）

## 使用示例

### 示例 1：完整功能开发

```
用户：我要添加用户登录功能，进入 Plan Mode，制定计划后保存到 plan.md
      然后按计划开发，每步更新 progress.txt，最后 review 代码

AI: [自动执行 cost-control → task-planner]
    📊 当前消耗: $0.012
    
    [进入 task-planner]
    📋 Execution Plan: 用户登录功能
    Est. total cost: $0.08 | ~12 rounds
    
    Phase 1: 数据库设计
    - 添加 User 模型 → Output: prisma/schema.prisma
    
    Phase 2: API 开发
    - 登录接口 → Output: app/api/auth/login/route.ts
    - 注册接口 → Output: app/api/auth/register/route.ts
    
    Phase 3: 前端页面
    - 登录页面 → Output: app/login/page.tsx
    
    Execute this plan? Or adjustments?

用户：开始执行

AI: [自动执行 workflow → doc-system → code-review → cost-control]
    Phase 1 完成 ✓
    [更新 progress.txt]
    
    Phase 2 完成 ✓
    [更新 progress.txt]
    
    Phase 3 完成 ✓
    [更新 progress.txt]
    
    [code-review] 代码审查结果...
    
    📊 本轮总消耗: $0.076
```

### 示例 2：Debug 修复

```
用户：报错了，帮我修复这个错误

AI: [自动执行 cost-control → debug-diagnosis]
    📊 当前消耗: $0.045
    
    [debug-diagnosis] 分析错误原因...
    根因：数据库连接超时
    修复方案：添加连接池配置
    
    [显示 diff]
    
用户：确认修复

AI: [自动执行 danger-signals → code-review → doc-system → git-commit → cost-control]
    ✓ 非第 3 次修复，继续
    [code-review] 验证通过
    [更新 progress.txt]
    [生成 commit message]
    📊 Debug 成本: $0.023
```

### 示例 3：健康检查

```
用户：/compact

AI: [自动执行 context-management]
    💡 上下文已压缩，保留关键信息
    当前进度: 用户登录功能开发中，Phase 2/3 完成
    
用户：检查健康度

AI: [自动执行 cost-control → danger-signals → doc-system → cost-control]
    📊 当前消耗: $0.089
    ⚠️ 接近 $0.10 阈值，建议新开会话
    🚨 检测到危险信号: 上下文占用 > 60%
    建议: /compact 或新开会话
    
    [更新 progress.txt]
    
    📊 会话成本总结: $0.089 / 15 轮
```

## 与 .clinerules 的关系

```
.clinerules (全局规则)
    ↓ 定义何时加载 skill-orchestrator
    
skill-orchestrator (意图识别)
    ↓ 匹配关键词，触发 skill 序列
    
individual skills (具体执行)
    ↓ 完成具体任务
```

在 `.clinerules` 中添加：

```markdown
## Skill 自动触发

当用户输入时，先由 skill-orchestrator 分析意图：
- 匹配触发词 → 执行对应 skill 序列
- 无匹配 → 正常对话
```
